export default class User {
  constructor(color, synth) {
    this.color = color;
    this.synth = synth;
  }

  setX(x) {
    this.x = x;
  }

  setY(y) {
    this.y = y;
  }

  setSynth(synth) {
    this.synth = synth;
  }
}
